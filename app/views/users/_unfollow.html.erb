<%= form_with(model: current_user.active_relationships.find_by(followed: @user),
              html: { method: :delete}) do |f| %>
  <%= f.submit "Unfollow", class: "rounded-full bg-zinc-100 text-zinc-800 font-medium px-2 py-1 cursor-pointer" %>
<% end %>